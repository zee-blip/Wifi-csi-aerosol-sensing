# Data Sample

Raw PCAP files and full feature datasets are not included due to file size limitations.

This folder is reserved for small sample files that demonstrate the expected data format, such as:

- Example CSI feature vectors
- Example labels
- Small CSV samples for edge inference testing
